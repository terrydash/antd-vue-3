import {defineComponent, ref} from 'vue';

export default defineComponent({
    name: 'approval',
    setup() {
        return () => (
            <>
                <div>
                    审批员管理
                </div>
            </>
        );
    },
});
